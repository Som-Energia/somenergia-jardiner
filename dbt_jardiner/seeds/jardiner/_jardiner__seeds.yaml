version: 2

seeds:
  - name: seed_plants__parameters
    description:
      some parameters of the plants, a subset of public.plantparameters and a few extra needed.
      It should replace public.plantparameters at some point
    config:
      column_types:
        is_tilted: boolean
        connection_date: timestamptz
